project:
    title: Tauri Client
    description: (Native) Client using Tauri

layer: application

language: typescript

toolchains:
    default: node

workspace:
    inheritedTasks:
        include:
            - pnpm
            - nuxt
            - build
            - lint

tasks:
    pnpm:
        args:
            - --filter
            - "@finance-tracker/apps-client"
